# Applications: runnable programs built on the engine. One subdirectory each;
# see README.md for the convention. Graphics-only, same as Renderer and UI.

add_subdirectory(SolarSystem)
add_subdirectory(LunarEclipse)
